package enums

const (
	ServerSideCheckFailure string = "Server-side check failed" // Server-side check failed
	ServerSideCheckOK      string = "Server-side check passed" // Server-side check passed

	InvalidAuthentication string = "Invalid authentication" // Invalid authentication
	ValidAuthentication   string = "Valid authentication"   // Valid authentication

	UserDeactivated string = "User is deactivated" // User is deactivated
	UserOK          string = "User is active"      // User is active
)
